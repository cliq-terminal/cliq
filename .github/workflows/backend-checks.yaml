name: Backend Checks
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# Sets a default working directory
defaults:
  run:
    working-directory: ./backend  # Change the working directory to /backend

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      checks: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Java 24
        uses: actions/setup-java@v4
        with:
          java-version: '24'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Install project
        run: bash ./app/install
      - name: Start Docker services
        run: docker compose up -d --wait
      - name: Run Gradle build
        run: ./gradlew bootJar
      - name: Run tests
        run: ./gradlew test
      # Publish Test Results are summary
      - name: Publish Test Results
        uses: dorny/test-reporter@v2
        if: ${{ !cancelled() }}       # run this step even if the previous step failed
        with:
          name: Backend Tests
          path: backend/**/build/test-results/test/TEST-*.xml
          reporter: java-junit
          working-directory: ./
#      - name: Deploy Test Report to GitHub Pages (PR only)
#        if: !cancelled() && github.event_name == 'pull_request'
#        uses: peaceiris/actions-gh-pages@v3
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_dir: backend/build/reports/tests/test
#          destination_dir: test-reports/pr-${{ github.event.number }}
#          keep_files: true
#      # Publish the Test HTML report and serve for 30 days
#      - name: Comment on PR with Test Report Link
#        if: !cancelled() && github.event_name == 'pull_request'
#        uses: actions/github-script@v7
#        with:
#          script: |
#            const reportUrl = `https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/test-reports/pr-${{ github.event.number }}/`;
#            const testStatus = '${{ job.status }}' === 'success' ? '✅' : '❌';
#
#            const body = `## ${testStatus} Test Report
#
#            📊 **HTML Test Report**: [View Report](${reportUrl})
#
#            _This report will be automatically deleted after 30 days._`;
#
#            // Find existing comment
#            const comments = await github.rest.issues.listComments({
#              owner: context.repo.owner,
#              repo: context.repo.repo,
#              issue_number: context.issue.number,
#            });
#
#            const existingComment = comments.data.find(comment =>
#              comment.user.login === 'github-actions[bot]' &&
#              comment.body.includes('Test Report')
#            );
#
#            if (existingComment) {
#              await github.rest.issues.updateComment({
#                owner: context.repo.owner,
#                repo: context.repo.repo,
#                comment_id: existingComment.id,
#                body: body
#              });
#            } else {
#              await github.rest.issues.createComment({
#                owner: context.repo.owner,
#                repo: context.repo.repo,
#                issue_number: context.issue.number,
#                body: body
#              });
#            }

  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Java 24
        uses: actions/setup-java@v4
        with:
          java-version: '24'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Run ktlint
        run: ./gradlew ktlintCheck
      - name: Annotate ktlint results
        if: ${{ !cancelled() }}
        uses: reviewdog/action-actionlint@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          reporter: github-pr-review
          ktlint_input: '**/build/reports/ktlint/ktlintMainSourceSetCheck/ktlintMainSourceSetCheck.xml'
